# Editable OpenRL math RL, inspired by vivekvkashyap/autoresearch-rl.
# Researchers tune only training knobs here. The OpenRL backend owns model choice and GPU placement.

max_steps = 4
seed = 0
batch_size = 8
rollouts_per_example = 2
max_tokens = 128
temperature = 1.0
lr = 0.000003
loss = "importance_sampling"
eval_enabled = true
eval_interval = 1
