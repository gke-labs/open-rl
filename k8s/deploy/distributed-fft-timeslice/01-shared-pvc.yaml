apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-rl-shared-pvc
  labels:
    app: open-rl
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: zonal-rwx
  resources:
    requests:
      storage: 10240Gi
