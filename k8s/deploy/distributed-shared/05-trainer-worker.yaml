apiVersion: v1
kind: Service
metadata:
  name: open-rl-trainer-worker-service
  labels:
    app: open-rl-trainer-worker
spec:
  selector:
    app: open-rl-trainer-worker
  ports:
  - port: 8000
    targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-rl-trainer-worker
  labels:
    app: open-rl-trainer-worker
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: open-rl-trainer-worker
  template:
    metadata:
      labels:
        app: open-rl-trainer-worker
    spec:
      serviceAccountName: open-rl-sa
      containers:
      - name: trainer-worker
        image: ghcr.io/gke-labs/open-rl/server:latest
        imagePullPolicy: Always
        command: ["uv", "run", "python", "-m", "server.training_requests_processor"]
        env:
        - name: ENABLE_GCP_TRACE
          valueFrom:
            configMapKeyRef:
              name: open-rl-config
              key: ENABLE_GCP_TRACE
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: CUDA_VISIBLE_DEVICES
          value: "0"
        - name: BASE_MODEL
          valueFrom:
            configMapKeyRef:
              name: open-rl-config
              key: BASE_MODEL
        - name: OPEN_RL_TMP_DIR
          value: "/mnt/shared/open-rl"
        - name: HF_HOME
          value: "/mnt/shared/open-rl/huggingface"
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 30 # Give it 5 minutes total to load the massive tensors from shared storage
        resources:
          limits:
            nvidia.com/gpu: "1"
            memory: "32Gi"
          requests:
            nvidia.com/gpu: "1"
            memory: "32Gi"
            cpu: "2"
        volumeMounts:
        - name: shared-storage
          mountPath: /mnt/shared
      volumes:
      - name: shared-storage
        persistentVolumeClaim:
          claimName: open-rl-shared-pvc
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-l4
      tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
